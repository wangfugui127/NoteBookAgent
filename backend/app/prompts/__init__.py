"""Central prompt catalog for NotebookAgent.

Prompt text belongs here instead of being embedded in runtime, ingestion, or
retrieval code. Dynamic values are supplied through small builder functions so
the executable modules remain focused on orchestration.
"""
